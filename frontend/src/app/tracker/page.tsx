export default function TrackerPage() {
  return (
    <main className="mx-auto max-w-[1600px] px-6 py-10">
      <header className="mb-8">
        <h1 className="text-2xl font-semibold text-paper">Degree Tracker</h1>
        <p className="text-sm text-muted">
          Track your progress and plan your prerequisite chains.
        </p>
      </header>
      
      {/* We will build the UI here */}
      <div className="rounded-2xl border border-hairline bg-panel p-6">
        <p className="text-sm text-muted">Tracker interface goes here...</p>
      </div>
    </main>
  );
}